# This module previously contained oracle/reference functions (gla_autograd_bwd_torch,
# gla_fwd_chunked_torch). Those have been moved to their respective test and benchmark
# files as part of the trust-model separation (no shared oracle surface).
