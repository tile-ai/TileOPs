# Version lock for CI / runner builds. Pins versions only; membership lives in pyproject
# (project dependencies) or the runner Dockerfile install lists (CI-only tooling).
# torch/torchvision/torchaudio bare (no `+cu132`): the CUDA variant comes from the install
# index, not this pin, so the same lock also fits the CPU preflight. Do not add `+cu132`.
# torch/torchvision are pinned to vllm 0.27.1's exact requirements, and are the newest pair
# the cu132 index publishes. torchaudio has no cu132 build at any version — it is pinned here
# only because vllm requires it; its wheel declares no dependencies, so pip takes it from
# PyPI without disturbing the cu132 torch. Nothing in TileOPs imports it.
torch==2.13.0
torchvision==0.28.0
torchaudio==2.11.0
# What torch 2.13.0 itself requires. Follow torch; never pick this independently.
triton==3.7.1
# apache-tvm-ffi is ABI-coupled to the baked tilelang wheel: it MUST sit inside the range
# tilelang's own metadata declares for the pinned commit (verified at build time by
# scripts/ci/verify_runtime_stack.py). Re-confirm this pin whenever TILELANG_GIT_SHA changes.
apache-tvm-ffi==0.1.11
cloudpickle==3.1.2
ml_dtypes==0.5.4
# vllm 0.27.1 takes numpy unconstrained; 2.2.6 satisfies tilelang (>=1.23.5).
numpy==2.2.6
psutil==7.2.2
tqdm==4.67.3
typing_extensions==4.15.0
Cython==3.2.4
z3-solver==4.15.4.0
torch_c_dlpack_ext==0.1.5
# CI tooling: versions locked here; the runner image install lists carry bare names.
pytest==9.0.2
pytest-xdist==3.8.0
ruff==0.14.13
pytest-timeout==2.3.1
# Stack dump of a hung benchmark child before the runner kills it.
py-spy==0.4.2
