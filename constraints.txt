# Versions for CI / runner builds, with the reasoning. Membership lives in pyproject (project
# deps) or the runner Dockerfile's install lists (CI-only tooling); the resolved closure lives in
# constraints-runner-lock.txt.
#
# No `+cu132`: the CUDA variant comes from the install index, so this file also fits the CPU
# preflight. torch/torchvision follow vllm 0.27.1's exact pins; see requirements.in for torchaudio.
torch==2.13.0
torchvision==0.28.0
torchaudio==2.11.0
# What torch 2.13.0 requires. Follow torch; never pick independently.
triton==3.7.1
# ABI-coupled to the baked tilelang wheel: MUST sit inside the range tilelang's metadata declares
# for the pinned commit (verify_runtime_stack.py checks it). Re-confirm on every TILELANG_GIT_SHA.
apache-tvm-ffi==0.1.11
cloudpickle==3.1.2
ml_dtypes==0.5.4
# vllm takes numpy unconstrained; 2.2.6 satisfies tilelang (>=1.23.5).
numpy==2.2.6
psutil==7.2.2
tqdm==4.67.3
typing_extensions==4.15.0
Cython==3.2.4
z3-solver==4.15.4.0
torch_c_dlpack_ext==0.1.5
# CI tooling; the Dockerfile install lists carry the bare names.
pytest==9.0.2
pytest-xdist==3.8.0
ruff==0.14.13
pytest-timeout==2.3.1
# Stack dump of a hung benchmark child before the runner kills it.
py-spy==0.4.2
# The installer. It cannot come from the lock it produces, and the same version must compile the
# lock and install from it.
uv==0.12.3
